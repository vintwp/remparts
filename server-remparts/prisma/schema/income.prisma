enum Currency {
  USD
  UAH
}

model Income {
  id        String @id @unique
  user      User @relation(fields: [userId], references: [id1c])
  userId    String @unique @map("user_id")
  createdAt DateTime
  value     Int
  currency  Currency

  @@map("income")
}